.row
  .container
    %h1.text-center Who do you want to find?
    .col-sm-6.col-sm-offset-3
      = search_form_for @q do |f|
        #{f.search_field :login_cont, style: 'width: 550px', :placeholder => 'type the login...'}
    %br/
    %br

  - @users.each do |user|
    .col-md-2
      .well.well-sm
        %p
          = image_tag("#{user.decorate.avatar_dec}", alt: "user avatar", style:"width:100px;height:100px;")
          = link_to user.login, user
